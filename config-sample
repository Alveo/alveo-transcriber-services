SQLALCHEMY_DATABASE_URI = 'sqlite:////tmp/test.db'
SQLALCHEMY_TRACK_MODIFICATIONS = False
SESSION_TYPE = 'filesystem'
CSRF_ENABLED = True

# Set to allow transcriber to access if hosted on a different domain
ACCESS_CONTROL_ALLOW_ORIGIN = '*'

SSAD_AGGRESSIVENESS = 2

MAX_CONTENT_LENGTH = 40 * 1024 * 1024 # After 40mb, throw RequestEntityTooLarge exception

DOWNLOAD_CACHE_PATH = '/tmp/alveo-transcriber-services/'

ALVEO_API_URL = "https://app.alveo.edu.au/"
DOMAIN_HANDLERS = [
  {
    "domains": ["app.alveo.edu.au"],
    "module": "alveo"
  }
]
